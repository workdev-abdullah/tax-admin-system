import Client from '../models/Client.js'; import Product from '../models/Product.js'; import Invoice from '../models/Invoice.js'; import Inventory from '../models/Inventory.js';
export async function summary(req,res){try{const {from,to}=req.query;const date={};if(from)date.$gte=new Date(from);if(to)date.$lte=new Date(`${to}T23:59:59`);const f=Object.keys(date).length?{invoiceDate:date,status:'ISSUED'}:{status:'ISSUED'};const [clients,products,invoices,sales,stock,low]=await Promise.all([Client.countDocuments({status:'ACTIVE'}),Product.countDocuments({isActive:true}),Invoice.countDocuments(f),Invoice.aggregate([{$match:f},{$group:{_id:null,sales:{$sum:{$toDouble:'$grandTotal'}},taxable:{$sum:{$toDouble:'$taxableTotal'}},cgst:{$sum:{$toDouble:'$cgstTotal'}},sgst:{$sum:{$toDouble:'$sgstTotal'}},igst:{$sum:{$toDouble:'$igstTotal'}}}}]),Inventory.aggregate([{$lookup:{from:'products',localField:'productId',foreignField:'_id',as:'p'}},{$unwind:'$p'},{$project:{value:{$multiply:['$quantity',{$toDouble:'$p.rate'}]},quantity:1,threshold:1}}]),Inventory.countDocuments({$expr:{$lte:['$quantity','$lowStockThreshold']}})]);const s=sales[0]||{};res.json({summary:{clients,products,invoices,sales:s.sales||0,taxable:s.taxable||0,cgst:s.cgst||0,sgst:s.sgst||0,igst:s.igst||0,stockValue:stock.reduce((a,x)=>a+(x.value||0),0),lowStock:low}});}catch(e){console.error(e);res.status(500).json({message:'Failed to load summary'});}}
