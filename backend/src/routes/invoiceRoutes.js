import {Router} from 'express';import {requireAdmin} from '../middleware/auth.js';import {listInvoices,getInvoice,createInvoice,issueInvoice,cancelInvoice} from '../controllers/invoiceController.js';const r=Router();r.use(requireAdmin);r.get('/',listInvoices);r.get('/:id',getInvoice);r.post('/',createInvoice);r.patch('/:id/issue',issueInvoice);r.patch('/:id/cancel',cancelInvoice);export default r;
